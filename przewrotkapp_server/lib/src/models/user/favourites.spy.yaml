class: FavouritesJunction
table: favourites_junctions
fields:
  gear: Gear?, relation(name=gear_favourites_junctions, onDelete=cascade)
  extraUserInfo: ExtraUserInfo?, relation(name=extra_user_info_junctions, onDelete=cascade)
indexes:
  favourites_junctions_index_idx:
    fields: gearId, extraUserInfoId
    unique: true
