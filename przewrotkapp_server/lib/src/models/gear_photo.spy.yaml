class: GearPhoto
table: gear_photos
fields:
  gear: Gear?, relation(optional, name=gear_photos, onDelete=Cascade)
  width: int
  height: int
  blurhash: String?
  path: String
indexes:
  gear_photos_gear_unique_idx:
    fields: gearId
    unique: false