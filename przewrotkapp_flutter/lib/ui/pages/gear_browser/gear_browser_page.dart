import 'package:flutter/material.dart';

class GearBrowserPage extends StatelessWidget {
  const GearBrowserPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Text("this page was shit :P so i destroyed it");
  }
}
